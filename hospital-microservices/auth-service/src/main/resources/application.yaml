server:
  port: 8085

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/hospital_auth?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root    # adapte avec ton password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  config:
    import: "optional:configserver:http://localhost:8888"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# JWT
jwt:
  secret: ZmFrZV9zZWNyZXRfa2V5X2l0X3Nob3VsZF9iZV9hdF9sZWFzdF8zMl9ieXRlcw
  expiration-ms: 3600000