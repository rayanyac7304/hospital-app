server:
  port: 8761

spring:
  application:
    name: discovery-server

# Un serveur Eureka standalone ne se registre pas lui-même
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
